{
  "name": "clinica-vida-lcode",
  "version": "2.0.0",
  "private": true,
  "description": "Clínica Vida+ — projeto de portfólio LCODE",
  "scripts": {
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check script.js && node --check painel-admin.js && node --check painel-medico.js && node --check painel-paciente.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mysql2": "^3.23.2"
  }
}
