const form=document.querySelector('#login-form');
form.addEventListener('submit',async e=>{e.preventDefault();const button=document.querySelector('#login-button');button.disabled=true;button.textContent='Entrando...';try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.querySelector('#email').value.trim(),senha:document.querySelector('#senha').value})});const d=await r.json();if(!r.ok)throw new Error(d.erro||'Não foi possível entrar.');window.appToast?.('Login realizado','success');const next=new URLSearchParams(location.search).get('next');setTimeout(()=>{if(d.usuario.tipo==='admin')location.href='painel-admin.html';else if(d.usuario.tipo==='medico')location.href='painel-medico.html';else location.href=next||'painel-paciente.html';},250);}catch(err){window.appToast?.(err.message,'error');}finally{button.disabled=false;button.textContent='Entrar';}});
